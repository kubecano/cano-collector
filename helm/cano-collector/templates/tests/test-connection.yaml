apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cano-collector.fullname" . }}-test-connection"
  labels:
    {{- include "cano-collector.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cano-collector.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
